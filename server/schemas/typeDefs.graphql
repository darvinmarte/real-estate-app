const typeDefs = `
  type User {
    _id: ID
    name: String
    email: String
    password: String
  }

  type Auth {
    token: ID!
    user: User
  }

  type Query {
    # queries will be here
  }

  type Mutation {
    # mutations will be here
  }
`;

module.exports = typeDefs;
